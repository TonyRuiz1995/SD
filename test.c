#include <stdio.h>
#include <math.h>

int main ()
{
   double x = sqrt(4.0);
   printf("power of 3 %lf to %lf\n", 4.0, sqrt(4.0) );
   printf("Square root of %lf is %lf\n", 5.0, sqrt(5.0) );
   printf(" the var is %lf\n",x);
   return(0);
}
